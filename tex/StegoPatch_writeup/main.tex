\documentclass[twocolumn]{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{hyperref}
\usepackage{newStyle}
\usepackage{titling}
\usepackage{stfloats}
\usepackage{algorithm}
\usepackage{algpseudocode}


\setlength{\droptitle}{-4em}

\title{StegoPatch Image Watermarking}
\author{Eugene Francisco}
\date{June 2026}

\begin{document}

\maketitle

\section{Overview}

Watermarking refers to the process of embedding information in generated data so as to label that data as having been generated by a model. This paper introduces StegoPatch, a novel way to watermark images, robust to common image transformations and lossy compression and virtually undetectable to the human eye. Inspired by Bui et al.~\cite{bui2023rosteals}, we train a \textit{message encoder} to embed hidden bit sequences in the latent representation of images from a fixed autoencoder; alongside this, we train a \textit{secret decoder} to recover messages from possibly corrupted images. We take advantage of convolutional autoencoders to allow for the spatial embedding of these watermarks; alongside this, we use techniques from Tancik et al.~\cite{tancik2020stegastamp} to make these watermarkers robust to spatial transformations like cropping and rotating, as well as regular image transformations like noising, blurring, and JPEG.

Our code can be found \href{https://github.com/EugeneFrancisco/StegoPatch}{here}.


\section{Related Work}
RoSteALS, introduced by Bui et al.~\cite{bui2023rosteals}, is the most direct inspiration for StegoPatch. Rather than learning an image-to-image encoder and decoder from scratch, RoSteALS watermarks within the latent space of a fixed, image autoencoder. A small neural network maps bit sequences to additive perturbations of the latent variable, which is then decoded back into image space. StegoPatch adopts this latent-space embedding strategy and extends it to embed messages spatially, exploiting the convolutional structure of the autoencoder so that the watermark is repeated across the image and can survive cropping.


Tancik et al.~\cite{tancik2020stegastamp} introduced StegaStamp. Their model allowed watermarks to survive the physical printing and recapture of an image. To accomplish this, StegaStamp is trained against a set of differentiable image perturbations like noise, blur, color shifts, and perspective warps, between the encoder and decoder during training, forcing the decoder to recover messages even from heavily corrupted inputs. StegoPatch borrows this idea of embedding a noise layer between encoding and decoding: we expose the secret decoder to spatial transformations like cropping and rotating, as well as noising, blurring, and JPEG compression, so that messages remain recoverable after these corruptions. Earlier, Zhu et al.~\cite{zhu2018hidden} proposed HiDDeN, one of the first end-to-end deep watermarking frameworks; we adopt their ideas for decoding messages by aggregating message predictions across the image by vote.
\begin{center}
	\begin{figure*}[h]
	\includegraphics[scale=0.4]{images/stella/title_picture_tex.png}
	\label{fig:title_image}
	\caption{StegoPatch is a \textit{post-hoc} watermarker, meaning it adds watermarks to existing images. StegoPatch works on images much larger than what it was originally trained on, like here, where StegoPatch has been used to watermark a Frank Stella painting that is $2\times$ larger resolution than used in training. Image courtesy of Christie's~\cite{christies2017stella}.}
\end{figure*}
\end{center}

\section{Methods}

Watermarking derives from the much older technique of steganography, where the challenge is to conceal messages within another medium. Stated concretely, the goal of image watermarking is to take a so-called \textit{cover image} $c$ and message $m$ and generate a \textit{stego image} $\tilde c$ in which $m$ is somehow embedded; later on, a verifier should be able to take $\tilde c$ (which has potentially been compressed or transformed in some way) and recover a message $\hat m$ which should be similar to $m$.

StegoPatch is a so-called \textit{post-hoc} watermarker, meaning it adds watermarks to existing images. This is as opposed to an \textit{ad-hoc} watermarker, which embeds messages into the generated content at the same time that the content is being generated.

\subsection{Architecture}
StegoPatch trains two neural networks:
\begin{enumerate}[label=(\roman*)]
\item A message encoder $F_\theta: \{0, 1\}^{\ell} \to \R^{P \times P \times C}$ which is used to encode messages and generate watermarked images.
\item A secret decoder $D_\varphi: \R^{H\times W\times C} \to \{0, 1\}^{\ell}$ which is used to recover a message $\hat m$ from a (possibly corrupted) $\tilde c$.	
\end{enumerate}

The message encoder $F_\theta$ takes advantage of a fixed image autoencoder to embed messages. Let $(E, G)$ be an image autoencoder. $E$ is an image encoder that sends images $c\in \R^{H\times W\times C}$ to $z = E(c) \in \R^{H'\times W'\times C}$ where $H', W'$ are a constant factor smaller than $H$ and $W$. And $G$ is an image generator that takes latent variables $z\in \R^{H'\times W'\times C}$ and sends them to $c = G(z) \in \R^{H\times W\times C}$. It is important that $(E, G)$ are convolutional so that they preserve spatial information. $(E, G)$ are fixed in advance and aren't trained.

The networks $F_\theta$ and $D_\varphi$ encode in the following way:

\begin{enumerate}[label=(\roman*)]
\item Message Encoder: given a cover image $c$ of size $H\times W$ and a message $m$ of length $\ell$, we first embed $c$ into its latent space to make latent variable $z \leftarrow E(c)$. Assign $\delta \leftarrow F_\theta(m)\in \R^{P\times P\times C}$; this will encode watermarked information. Partition $z$ into patches of size $P\times P\times C$; let $\P$ be the set of these patches.

For each patch $p\in \P$, create the watermarked patch $\tilde p \leftarrow p + \delta$. Using the watermarked patches, stitch together a watermarked latent $\tilde z$. Finally, return $G(\tilde z)$ as the watermarked cover. See Algorithm~\ref{alg:encoder}.

The neural network $F_\theta$ is a very small convolutional neural network implemented in a similar way to Bui et al.
\item Secret Decoder: Given a (possibly corrupted) watermarked image $\tilde c$, the secret decoder returns $\hat m = D_\varphi(\tilde c)$. The neural network $D_\varphi$ is implemented as a modified ResNet50 with the final flattening layer replaced by a $1\times 1$ CNN whose output is $\ell$ channeled. An average is taken across the width and height dimensions to retrieve an $\ell$ length vector of logits which are thresholded and returned as $\hat m$. See Algorithm~\ref{alg:decoder}

This last \textit{voting} step is taken from Zhu et al.'s HiDDeN~\cite{zhu2018hidden}.
\end{enumerate}
\begin{center}
	\begin{figure*}
	\includegraphics[scale=0.32]{images/architecture_plot.png}
	
	\caption{StegoPatch Watermarking and Decoding Architecture. The $\rm{message\ encoder}$ refers to $F_\theta$ and the $\rm{secret\ decoder}$ refers to $D_\varphi$.}
\end{figure*}
\end{center}


Here, $\theta$ and $\varphi$ are trainable parameters; note in particular that the autoencoder $(E, G)$ remain fixed and are not trained.

In this architecture, it is important that the patching and periodic pasting of the latent variable $\delta$ happens within the latent space. Watermarking performance worsens significantly if instead we choose to watermark each patch individually and then stitch together the patches in image space. Doing so leads to more obvious stitch marks and much higher quality loss during training.

\begin{algorithm}
\caption{Message Encoder $F_\theta$}
\label{alg:encoder}
\begin{algorithmic}[1]
\Require cover image $c \in \R^{H\times W\times C}$, message $m \in \{0,1\}^\ell$
\Ensure stego image $\tilde c$
\State $z \gets E(c)$ 
\State $\delta \gets F_\theta(m)$ 
\State $\P \gets \textproc{Partition}(z)$ \Comment{into $P\times P\times C$ patches}
\For{each patch $p \in \P$}
    \State $\tilde p \gets p + \delta$
\EndFor
\State $\tilde z \gets \textproc{Stitch}(\{\tilde p\})$
\State \Return $G(\tilde z)$
\end{algorithmic}
\end{algorithm}

\begin{algorithm}
\caption{Secret Decoder $D_\varphi$}
\label{alg:decoder}
\begin{algorithmic}[1]
\Require (possibly corrupted) stego image $\tilde c \in \R^{H\times W\times C}$
\Ensure recovered message $\hat m \in \{0,1\}^\ell$
\State $L \gets \textproc{ModifiedResNet50}_\varphi(\tilde c)$ \Comment{Last layer is a $1\times 1\times$ CNN, $\ell$ channels. $L\in \R^{H'\times W'\times \ell}$}
\State $\bar L \gets \textproc{Mean}_{H',W'}(L)$
\State $\hat m \gets \mathbf{1}[\bar L > 0]$
\State \Return $\hat m$
\end{algorithmic}
\end{algorithm}
\subsection{Training Details}
\begin{figure*}
	\centering
	\setlength{\tabcolsep}{1pt}
	\renewcommand{\arraystretch}{0.5}
	\newcommand{\resultcell}[1]{%
		\includegraphics[width=0.19\textwidth]{#1}}
	\begin{tabular}{ccccc}
		Cover & Watermarked & JPEG & Crop & Rotate \\
		\resultcell{images/cat/cover.png} &
		\resultcell{images/cat/watermarked.png} &
		\resultcell{images/cat/jpeg_compression_severity_1.png} &
		\resultcell{images/cat/crop.png} &
		\resultcell{images/cat/rotate_angle_30.0.png} \\
		\resultcell{images/elephants/cover.png} &
		\resultcell{images/elephants/watermarked.png} &
		\resultcell{images/elephants/jpeg_compression_severity_1.png} &
		\resultcell{images/elephants/crop.png} &
		\resultcell{images/elephants/rotate_angle_30.0.png} \\
		\resultcell{images/flower/cover.png} &
		\resultcell{images/flower/watermarked.png} &
		\resultcell{images/flower/jpeg_compression_severity_1.png} &
		\resultcell{images/flower/crop.png} &
		\resultcell{images/flower/rotate_angle_30.0.png} \\
		\resultcell{images/kite/cover.png} &
		\resultcell{images/kite/watermarked.png} &
		\resultcell{images/kite/jpeg_compression_severity_1.png} &
		\resultcell{images/kite/crop.png} &
		\resultcell{images/kite/rotate_angle_30.0.png} \\
		\resultcell{images/planes/cover.png} &
		\resultcell{images/planes/watermarked.png} &
		\resultcell{images/planes/jpeg_compression_severity_1.png} &
		\resultcell{images/planes/crop.png} &
		\resultcell{images/planes/rotate_angle_30.0.png} \\
	\end{tabular}
	\caption{Qualitative results. Each row is an image class (cat, elephants,
	flower, kite, planes) and each column is a transformation applied to the
	watermarked image (cover and watermarked shown for reference). Watermarked images without any noise applied achieve $100\%$ bit accuracy.}
	\label{fig:results}
\end{figure*}

We use the same training objectives as RoSteALS~\cite{bui2023rosteals}. $F_\theta$ is trained to minimize
\begin{align*}
	\L_{\mathrm{quality}} = \L_{\mathrm{LPIPS}}(\tilde c, c) + \alpha \Vert\gamma(c) - \gamma(\tilde c)\Vert_2^2\tag{1}
\end{align*}
where $\L_{\mathrm{LPIPS}}$ is the \textit{Learned Perceptual Image Patch Similarity} loss from Zhang et al.~\cite{zhang2018lpips} and $\gamma$ is a differentiable map from RGB to YUV color space. $\alpha$ is a hyperparameter to control objective weight. $D_\varphi$ is trained to minimize the binary cross entropy between $m$ and $\hat m$:
\begin{align*}
	\L_{\mathrm{recovery}} = \L_{\mathrm{BCE}}(m, \hat m).\tag{2}
\end{align*}
The combined training objective is then
\begin{align*}
	\L = \beta \L_{\mathrm{quality}} + \L_{\mathrm{recovery}}\tag{3}
\end{align*}
where $\beta$ is a weighting hyperparameter.




We use curriculum learning to train $F_\theta$ and $D_\varphi$. Learning is split into the following phases:
\begin{enumerate}
	\item Fix a single minibatch of images from the dataset. Train $F_\theta$ and $D_\varphi$ on this minibatch until bit accuracy of $m$ against $\hat m$ crosses $t_1$. Sample $m$'s bits uniformly and randomly. Before passing $\tilde c$ into $D_\varphi$, we choose with probability $1/2$ whether to crop $\tilde c$, in which case we crop $\tilde c$ to a random patch with sides of at least $3P$ in length.
	\item Expose $F_\theta$ and $D_\varphi$ to a much larger portion of the training set ($50{,}000$ images in our case). Train until bit accuracy reaches $t_2$ as this new exposure will likely drop the bit accuracy by a significant amount.
	\item Begin incrementing $\beta$ linearly by some amount $\delta_\beta$ until it reaches some $\beta_{\mathrm{max}}$, at which point continue training until bit accuracy reaches $t_3$.
	\item Reveal the full dataset to $F_\theta$ and $D_\varphi$ and train until bit accuracy reaches $t_4$.
	\item Introduce a noise function $N$ which manipulates $\tilde c$ to generate a noised image $N(\tilde c)$. Pass $N(\tilde c)$ into $D_\varphi$. Train for an additional $k$ epochs.
\end{enumerate}


\section{Implementation Details}
For simplicity, we trained on square images of size $384\times 384$ using a patch size of $P = 96$. All of our experiments are done with a message length of $\ell = 20$. We picked $\alpha = 1.5$ as in RoSteALS. We chose $\beta = 0.8$ and increment $\beta$ linearly until $\beta_{\mathrm{max}} = 15$ in training phase 3. The bit accuracy thresholds are set to $t_1 = 0.9, t_2 = 0.8, t_3 = 0.95, t_4 = 0.98$. The last training phase we trained for $k = 3$ epochs. We used the first $100{,}000$ images from the COCO training dataset from 2017~\cite{lin2014coco}. We optimize the objective in (3) using stochastic gradient descent (in PyTorch), a minibatch size of $8$, and the AdamW optimizer with a learning rate of $2\cdot 10^{-5}$. Training was done on an NVIDIA A100 40GB GPU and took around 10 hours per experiment.

We use a very small neural network for $F_\theta$ with only $9{,}168$ parameters. The network is described by the following PyTorch Sequential network:
\begin{verbatim}
nn.Linear(20, 12 * 12 * 3),
nn.SiLU(),
nn.Unflatten(1, (3, 12, 12)),
nn.Upsample(scale_factor=2),
nn.Conv2d(3, 3, 3, padding = 1),
nn.Conv2d(3, 3, 1)
\end{verbatim}
where, as in RoSteALS, the final layer is $0$-initialized so that $\delta = 0$ at the start training.

We use the vq-f4 autoencoder~\cite{esser2021vqgan} just like RoSteALS; notably, this autoencoder is fully convolutional which means that periodic patching in the latent space will be preserved in the image space.

We use several different noise transformations which can be seen in Figure~\ref{fig:all_noises}. For those noise transformations which are non-differentiable, we use \cite{bui2023rosteals}'s trick of treating the noise as a constant by letting $N(c) = c + \langle N(c) - c \rangle$ where $\langle \cdot \rangle$ is treated as a constant (no computational graph or gradients computed); we also use \cite{bui2023rosteals} for the \textit{differentiable} noise that is added, which is a layered combination of noises that is differentiable.


\section{Results}
\begin{figure}
	\includegraphics[scale=0.40]{images/stegosaurus/cover.png}\includegraphics[scale=0.40]{images/stegosaurus/watermarked.png}
	\label{fig:stegosaurus}
	\caption{StegoPatch applied to an image with text.}
\end{figure}


Figure~\ref{fig:results} shows qualitative results across five image classes (rows) under a range of transformations (columns). For each class we show the original cover image, the watermarked image, and the watermarked image after JPEG compression, cropping, and rotation. The watermarked images are essentially identical to the original images, except in the case of the flower (row 3) where the white background betrays some faint watermarking splotches. Also notable is that all the images displayed here use a resolution $1.5\times$ larger than the one used for training (and Figure~\ref{fig:title_image} is $2\times$ larger), demonstrating scalability to unseen dimensions.

Interestingly, StegoPatch seems to perform better on text (Figure~\ref{fig:stegosaurus}) and faces than \cite{bui2023rosteals} even though both utilize an image encoder backbone. It is important that the patching for StegoPatch happens within the latent space since otherwise clear stitch marks can be seen between the patches, as can be seen in Figure~\ref{fig:faces}.
\begin{figure}
	\includegraphics[scale=0.5]{images/face/cover.png}\\
	\includegraphics[scale=0.34]{images/face/face_watermarked_v1_plot.png}\\
	\includegraphics[scale=0.5]{images/face/watermarked.png}
	\label{fig:faces}
	\caption{A comparison of watermarking when stitching is done in the image space (middle) vs latent space (bottom). Clear stitch marks can be seen in the middle on the left person's face.}
\end{figure}

StegoPatch achieves very strong performance on most forms of image manipulation, as can be seen in Figure~\ref{fig:whiskers}. We point in particular to JPEG performance where StegoPatch achieves performance comparable or stronger than \cite{bui2023rosteals} and \cite{tancik2020stegastamp}.
\begin{figure*}[t]
	\includegraphics[scale=0.5]{images/noise_robustness_whisker.png}
	\label{fig:whiskers}
	\caption{A whisker plot of StegoPatch's performance against different image manipulations.}
\end{figure*}
However, StegoPatch still has notable weaknesses. For example, although StegoPatch does significantly better than guessing when crops are applied, it still has a statistical power of only around $0.75$ (crops are sampled as a random height and random width, where height and widths must be a minimum of $3$ patch-lengths). We note though that this power grows with image size as more patches become visible. Another disadvantage of StegoPatch is that, applied to images that have much larger resolution than what is trained on, StegoPatch seems to impart more discoloring on the image. Also, on images that have little detail, StegoPatch struggles to recover images that have been JPEG compressed (see future work).
\section{Future Work}
Training StegoPatch on even larger, higher resolution images, will likely allow for much stronger robustness against cropping. Part of the challenge with training StegoPatch is that training images cannot be significantly larger than their cropped counterparts due to the author's compute limitations. While we used images that were $4\times 4$ patches across with a maximum crop down to $3\times 3$ patches, an ideal setting would train StegoPatch on larger images where more patches can be spread out.

Along these lines, it may be interesting to see whether patch placement can be chosen more carefully rather than pure periodicity. One may be able to choose the places in the image where the watermark would be more hidden to place a stronger watermark. For example, the message encoder $F_\theta$ could take in the cover image too as input to give it context for how much to watermark each patch, as in StegaStamp~\cite{tancik2020stegastamp}.


\begin{thebibliography}{9}

\bibitem{bui2023rosteals}
Tu Bui, Shruti Agarwal, Ning Yu, and John Collomosse.
\newblock RoSteALS: Robust steganography using autoencoder latent space.
\newblock In \textit{Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) Workshops}, 2023.

\bibitem{esser2021vqgan}
Patrick Esser, Robin Rombach, and Bj\"orn Ommer.
\newblock Taming transformers for high-resolution image synthesis.
\newblock In \textit{Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)}, 2021.

\bibitem{tancik2020stegastamp}
Matthew Tancik, Ben Mildenhall, and Ren Ng.
\newblock StegaStamp: Invisible hyperlinks in physical photographs.
\newblock In \textit{Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)}, 2020.

\bibitem{zhu2018hidden}
Jiren Zhu, Russell Kaplan, Justin Johnson, and Li Fei-Fei.
\newblock HiDDeN: Hiding data with deep networks.
\newblock In \textit{Proceedings of the European Conference on Computer Vision (ECCV)}, 2018.

\bibitem{lin2014coco}
Tsung-Yi Lin, Michael Maire, Serge Belongie, James Hays, Pietro Perona, Deva Ramanan, Piotr Doll\'ar, and C.~Lawrence Zitnick.
\newblock Microsoft COCO: Common objects in context.
\newblock In \textit{Proceedings of the European Conference on Computer Vision (ECCV)}, 2014.

\bibitem{zhang2018lpips}
Richard Zhang, Phillip Isola, Alexei A. Efros, Eli Shechtman, and Oliver Wang.
\newblock The unreasonable effectiveness of deep features as a perceptual metric.
\newblock In \textit{Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)}, 2018.

\bibitem{christies2017stella}
Christie's.
\newblock 15 things to know about Frank Stella.
\newblock \href{https://www.christies.com/en/stories/15-things-to-know-about-frank-stella-cdb2d362bac44f7fb7567e26ac2e201e}{\underline{linked here}}, accessed 2026.

\end{thebibliography}

\begin{figure*}[p]
	\centering
	\setlength{\tabcolsep}{2pt}
	\renewcommand{\arraystretch}{0.6}
	\newcommand{\noisedir}{../../data/sample_images/cat/000000236877_noised/000000236877_}
	\newcommand{\noisecell}[2]{%
		\begin{tabular}{@{}c@{}}
			\includegraphics[width=0.15\textwidth]{\noisedir #1.png} \\
			\scriptsize #2
		\end{tabular}}
	\begin{tabular}{cccccc}
		\noisecell{identity}{Identity} &
		\noisecell{brightness}{Brightness} &
		\noisecell{contrast}{Contrast} &
		\noisecell{saturate}{Saturate} &
		\noisecell{fog}{Fog} &
		\noisecell{spatter}{Spatter} \\
		\noisecell{gaussian_noise}{Gaussian noise} &
		\noisecell{shot_noise}{Shot noise} &
		\noisecell{impulse_noise}{Impulse noise} &
		\noisecell{speckle_noise}{Speckle noise} &
		\noisecell{gaussian_blur}{Gaussian blur} &
		\noisecell{defocus_blur}{Defocus blur} \\
		\noisecell{pixelate}{Pixelate} &
		\noisecell{elastic_transform}{Elastic transform} &
		\noisecell{rotate}{Rotate} &
		\noisecell{crop}{Crop} &
		\noisecell{resized384}{Resized 384} &
		\noisecell{differentiable}{Differentiable} \\
		\noisecell{jpeg_compression_sev1}{JPEG sev.\ 1} &
		\noisecell{jpeg_compression_sev2}{JPEG sev.\ 2} &
		\noisecell{jpeg_compression_sev3}{JPEG sev.\ 3} &
		\noisecell{jpeg_compression_sev4}{JPEG sev.\ 4} &
		\noisecell{jpeg_compression_sev5}{JPEG sev.\ 5} \\
	\end{tabular}
	\caption{The full set of image manipulations (noises) applied to a single cover image during training and evaluation. Each panel shows the same image after a different distortion.}
	\label{fig:all_noises}
\end{figure*}

\begin{figure*}
	\includegraphics[scale=0.5]{images/metrics/beta.png}\includegraphics[scale=0.5]{images/metrics/bit_accuracy.png}\\
	\includegraphics[scale=0.5]{images/metrics/delta_l2.png}
	\includegraphics[scale=0.5]{images/metrics/loss_quality.png}
	\caption{Performance curves during training. Training was done over the course of three runs.}
\end{figure*}



























\end{document}